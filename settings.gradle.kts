rootProject.name = "calculation-engine-service"

include(
    "domain",
    "api",
    "application",
    "observability-adapter",
    "web-client-adapter",
    "cache-adapter",
    "rest-adapter",
    "bootstrap"
)
