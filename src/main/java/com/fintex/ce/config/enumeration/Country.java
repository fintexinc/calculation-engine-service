package com.fintex.ce.config.enumeration;

/**
 * this enum is used for mapping data from fds particularly businessCountry
 * EMPTY enum means that there is no data from fds of businessCountry
 */
public enum Country {
    CAN,
    USA,
    OTHER,
    EMPTY
}
